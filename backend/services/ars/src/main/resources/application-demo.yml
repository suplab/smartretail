# Demo profile: JWT is parsed so DashboardController.extractRoles() can read
# cognito:groups from the token (scp1@test.com → SC_PLANNER group).
# oauth2ResourceServer is enabled in SecurityConfig#demoSecurity; the JwtDecoder
# is auto-configured from the issuer-uri set in application-aws.yml (loaded via
# profiles.group.demo: [aws]). OIDC discovery against Cognito works reliably.
