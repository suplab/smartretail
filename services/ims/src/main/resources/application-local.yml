spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartretail?currentSchema=inventory
    username: smartretail_admin
    password: local_dev_password
  cloud:
    aws:
      parameterstore:
        enabled: false
      secretsmanager:
        enabled: false
      sqs:
        enabled: false

smartretail:
  eventbridge:
    bus-name: mock-local
  mock:
    re-url: http://localhost:8082
